package com.example.bingeme.di

/**
 * Represents the Dagger-Hilt module for providing application-level dependencies.
 * This class should be used to define bindings for dependencies that are used across the application.
 *
 * Currently, this class is empty but can be expanded to include methods annotated with `@Provides` or `@Binds`
 * to supply dependencies such as repositories, services, or utility classes.
 */
class AppModule {
    // Add dependency providers here for application-level objects
}
